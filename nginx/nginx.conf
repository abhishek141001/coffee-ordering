upstream web_upstream {
    server web:3000;
}

upstream backend_upstream {
    server backend:5000;
}

# Redirect HTTP to HTTPS
server {
    listen 80;
    server_name ordercoffee.online www.ordercoffee.online;

    # Let's Encrypt challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

# Main HTTPS server
server {
    listen 443 ssl;
    server_name ordercoffee.online www.ordercoffee.online;

    ssl_certificate /etc/letsencrypt/live/ordercoffee.online/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/ordercoffee.online/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    # Proxy headers
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    # API routes -> backend (strips /api/ prefix)
    location /api/ {
        proxy_pass http://backend_upstream/;
        client_max_body_size 10m;
    }

    # Everything else -> Next.js
    location / {
        proxy_pass http://web_upstream;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
